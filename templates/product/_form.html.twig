{#{{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}
{#    <button class="btn">{{ button_label|default('Сохранить') }}</button>#}
{#{{ form_end(form) }}#}


{% block body %}
   {{ form_start(form) }}
   {{ form_widget(form) }}
{{ form_end(form) }}
       
{% endblock %}